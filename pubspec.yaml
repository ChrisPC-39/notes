name: notes
description: Offline note keeping app.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.8.1+5

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.0.2
  hive_flutter: ^1.0.0
  path_provider: ^2.0.1
  animations: ^2.0.0
  share: ^2.0.1
  url_launcher: ^5.7.5
  focused_menu: ^1.0.5
  flutter_colorpicker: ^0.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.0.1
  build_runner: ^1.10.11

flutter:
  uses-material-design: true